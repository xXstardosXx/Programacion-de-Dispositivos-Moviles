export const environment = {
  production: false,
  // Desde el teléfono NO uses localhost (apunta al propio móvil).
  // Usa la IP LAN de tu PC (la misma que ves en la barra del navegador del teléfono).
  // En el navegador del PC también funciona con esta IP.
  apiUrl: 'http://192.168.1.228:4001/api',
};
